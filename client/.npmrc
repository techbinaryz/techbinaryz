# Enforce the Node/pnpm versions declared in package.json "engines"
engine-strict=true

# Peer deps: resolve automatically, but don't fail installs on conflicts.
auto-install-peers=true
strict-peer-dependencies=false
